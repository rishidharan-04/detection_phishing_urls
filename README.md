# detection_phishing_urls
This project focuses on building a machine learning-based system to detect phishing URLs using a robust Gradient Boosting Classifier. It includes end-to-end implementation—from dataset preprocessing, feature engineering, model training, evaluation, and deployment via a real-time Telegram chatbot.
